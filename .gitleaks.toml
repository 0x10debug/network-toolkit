# Gitleaks configuration for network-toolkit.
# Extends the default rule set and only narrows documented endpoints.

[extend]
useDefault = true

[allowlist]
  description = "Documentation-only config template; the flagged match is the Authelia forward-auth endpoint (authelia:9091), not a credential"
  paths = [
    '''templates/sso/authelia\.yml''',
  ]
